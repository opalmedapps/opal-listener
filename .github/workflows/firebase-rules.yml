name: Firebase Rules
run-name: Working on deployment of Firebase rules 🚀
on:
  push:
    branches:
      - OP-93.SB.github-actions
#    paths:
#      - 'firebase/dev/database.rules.json'
# TODO restrict to main branch

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.14.X'
      - run: npm -v
      - name: Install firebase-tools
        run: |
          npm install -g firebase-tools
          firebase --version

      - name: Read the service account needed to deploy to Firebase
        run: |
          echo '${{ secrets.DEV_GOOGLE_APPLICATION_CREDENTIALS }}' > google-application-credentials.json
          echo "GOOGLE_APPLICATION_CREDENTIALS=`readlink -f google-application-credentials.json`" >> $GITHUB_ENV
      - run: cat $GOOGLE_APPLICATION_CREDENTIALS | grep -o '\\n' | wc -l
      - run: cat $GOOGLE_APPLICATION_CREDENTIALS | wc -c
      - name: Deploy rules to Firebase
        working-directory: ./firebase/dev
        run: |
          echo $GOOGLE_APPLICATION_CREDENTIALS
          cat $GOOGLE_APPLICATION_CREDENTIALS
          firebase deploy --only database
