version: '3'

services:
  app:
    container_name: opal-listener
    image: opalmedapps/listener:latest
    build: .
    ports:
      - 8080:80
    volumes:
      - ./legacy-listener:/app/legacy-listener
      - ./src:/app/src
    command: npm run watch:docker
